name: Continuous Integration and Deployment

on:
  push:
    branches:
      - '**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: docker.io/jpplay/auronix-server
  JAVA_VERSION: "26"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate application
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: maven

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Make Maven wrapper executable
        run: chmod +x ./mvnw

      - name: Resolve dependencies
        run: ./mvnw -B dependency:go-offline

      - name: Run tests
        run: ./mvnw -B test

      - name: Require Testcontainers integration tests
        run: |
          docker ps
          ./mvnw -B -Dtest=FinancePostgresIntegrationTest test
          report="target/surefire-reports/TEST-dev.joaopdias.auronix.integration.FinancePostgresIntegrationTest.xml"
          test -f "$report"
          skipped="$(grep -o 'skipped="[0-9]*"' "$report" | head -n 1 | cut -d '"' -f 2)"
          failures="$(grep -o 'failures="[0-9]*"' "$report" | head -n 1 | cut -d '"' -f 2)"
          errors="$(grep -o 'errors="[0-9]*"' "$report" | head -n 1 | cut -d '"' -f 2)"
          test "$skipped" = "0"
          test "$failures" = "0"
          test "$errors" = "0"

      - name: Build application
        run: ./mvnw -B package -DskipTests

      - name: Build Docker image
        run: docker build -t auronix-server:${{ github.sha }} .

      - name: Validate Kubernetes manifests offline
        shell: pwsh
        run: ./scripts/validate-k8s-offline.ps1

      - name: Validate Terraform offline
        shell: pwsh
        run: ./scripts/validate-terraform.ps1

  publish:
    name: Publish Docker image
    needs: validate
    runs-on: ubuntu-latest
    outputs:
      image_digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=sha,format=long

      - name: Build and push
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha,scope=auronix-server
          cache-to: type=gha,mode=max,scope=auronix-server

  deploy:
    if: github.ref == 'refs/heads/main'
    name: Deploy Application
    runs-on: self-hosted
    needs: publish
    timeout-minutes: 15
    env:
      DOTENV: ${{ secrets.DOTENV }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Write dotenv to file
        run: |
          printf '%s\n' "$DOTENV" > .env
          chmod 600 .env

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Pull images
        run: docker pull "$IMAGE_NAME@${{ needs.publish.outputs.image_digest }}"

      - name: Deploy server container
        run: |
          docker rm -f auronix-server || true
          docker run -d \
            --name auronix-server \
            --restart=always \
            --network cloudflare_tunnel \
            --env-file .env \
            --security-opt no-new-privileges:true \
            --cap-drop ALL \
            --memory=1024m \
            --memory-swap=1024m \
            --cpus=1.0 \
            --pids-limit=256 \
            "$IMAGE_NAME@${{ needs.publish.outputs.image_digest }}"

      - name: Remove unused images
        run: docker image prune -f -a
